	
	// Hide Address bar when device is orientated to landscape
	//window.document.addEventListener('orientationchange', function(){
	  //if (window.orientation === 90 || window.orientation === -90){
	   // window.scrollTo(0, 0);
	  //}
	//}, false);